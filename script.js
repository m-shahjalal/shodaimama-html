console.log('hello shodaimama');

const images = []
for (let i = 0; i <= 100; i++) {
   const imageName = `imag_${i}`
   import imageName from `../images/folder1/sadldf-000${i}`
    images.push(imageName)
}
